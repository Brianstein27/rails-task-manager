<h1><%= @task.title %></h1>
<h2>
  Details
</h2>
<p><%= @task.details %><p>
    <h2>Done?</h2>
    <p><%= @task.completed %></p>

    <p>
      <div><%= link_to "back to list", tasks_path %></div>
      <div><%= link_to "Delete",
        task_path(@task),
        data: {
          turbo_method: :delete,
          turbo_confirm: "Are you sure?",
        } %>
        <div><%= link_to "Edit", edit_task_path(@task) %>
